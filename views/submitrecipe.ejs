<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>CulinaryCove - Submit Recipe</title>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">CulinaryCove</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <input type="text" id="searchInput" class="form-control mb-3" placeholder="Search for recipes..." style="max-width:500px;">
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/viewrecipe">View All Recipes</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/submitrecipe">Submit Recipe</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/addingredient">Add Ingredient</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/profile">Delete Recipe</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>
    <div class="container mt-5">
        <h1>Submit a New Recipe</h1>
        <form action="/submitrecipe" method="POST">
            <div class="mb-3">
                <label for="recipeTitle" class="form-label">Recipe Title</label>
                <input type="text" class="form-control" id="recipeTitle" name="title" required>
            </div>
            <div class="mb-3">
                <label for="category" class="form-label">Category</label>
                <select name="category" id="category" class="form-select">
                    <option value="1">hors d'oeuvre</option>
                    <option value="2">Tantalizing Main Courses</option>
                    <option value="3">Savory Creations</option>
                </select>
            </div>
            <div class="mb-3">
                <label for="recipeIngredients" class="form-label">Ingredients</label>
                <p class="text-muted"><small>If ingredient is not found, please add ingredient first</small></p>
                <div class="row">
                    <% for(let i = 0; i < ingredient.length; i++) { %>
                        <div class="col-md-3 mb-3">
                            <div class="form-check">
                                <input type="checkbox" class="form-check-input" id="ingredient<%= i %>" name="ingredient" value="<%= ingredient[i].ingredient_id %>">
                                <label class="form-check-label" for="ingredient<%= i %>"><%= ingredient[i].name %></label>
                            </div>
                            <div class="input-group input-group-sm mb-2">
                                <span class="input-group-text" id="measureLabel<%= i %>">Quantity</span>
                                <input type="number" class="form-control" id="measure<%= i %>" name="measure" aria-describedby="measureLabel<%= i %>">
                            </div>
                            <div class="input-group input-group-sm">
                                <span class="input-group-text" id="unitLabel<%= i %>">Unit</span>
                                <input type="text" class="form-control" id="unit<%= i %>" name="unit" aria-describedby="unitLabel<%= i %>">
                            </div>
                        </div>
                        <% if ((i + 1) % 4 === 0 || i === ingredient.length - 1) { %>
                    </div>
                    <div class="row">
                        <% } %>
                    <% } %>
                </div>
            </div>
            <div class="mb-3">
                <label for="recipeDescription" class="form-label">Description</label>
                <textarea class="form-control" id="recipeDescription" rows="5" name="description" required></textarea>
            </div>
            <div class="mb-3">
                <label for="recipeInstructions" class="form-label">Instructions</label>
                <textarea class="form-control" id="recipeInstructions" rows="5" name="instruction" required></textarea>
            </div>
            <button type="submit" class="btn btn-primary">Submit</button>
        </form>
    </div>
</body>

</html>
