<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/css/bootstrap.min.css" rel="stylesheet">
    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.2/dist/js/bootstrap.bundle.min.js"></script>
    <title>CulinaryCove - Recipe Details</title>
</head>

<body>
    <nav class="navbar navbar-expand-lg navbar-dark bg-primary">
        <div class="container">
            <a class="navbar-brand" href="/">CulinaryCove</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <input type="text" id="searchInput" class="form-control mb-3" placeholder="search for recipes..." style="max-width:500px;">
            <div class="collapse navbar-collapse" id="navbarNav">
                <ul class="navbar-nav ms-auto">
                    <li class="nav-item">
                        <a class="nav-link" href="/">Home</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/viewrecipe">View All Recipe</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/submitrecipe">Submit Recipe</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/addingredient">Add Ingredient</a>
                    </li>
                    <li class="nav-item">
                        <a class="nav-link" href="/profile">Delete Recipe</a>
                    </li>
                </ul>
            </div>
        </div>
    </nav>

    <div class="container my-5">
        <h2 class="mb-4">Recipe Details</h2>

        <div class="card">
            <div class="card-body">
                <h5 class="card-title"><%= recipe[0].title %></h5>
                <h6 class="card-subtitle mb-2 text-muted">Ingredients:</h6>
                <ul class="list-group list-group-flush">
                    <% for(let i=0; i < recipe.length; i++) { %>
                    <li class="list-group-item"><%= recipe[i].name %> - <%= recipe[i].Quantity %> <%= recipe[i].Unit %></li>
                    <% } %>
                </ul>
                <p class="card-text"><strong>Description: </strong><%= recipe[0].description %></p>
                <p class="card-text"><strong>Instructions:</strong> <%= recipe[0].instructions %></p>
                <p class="card-text"><small class="text-muted">Created on: <%= recipe[0].created_on %></small></p>
                <p class="card-text"><small class="text-muted">Updated on: <%= recipe[0].updated_on %></small></p>
                <a href="/" class="btn btn-primary">Back</a>
            </div>
        </div>
    </div>
</body>

</html>